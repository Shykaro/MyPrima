namespace Script {
    import ƒ = FudgeCore;

    export class Balance extends ƒ.Node {
        static costOfMobs: any; //Sorry but we just gonna do this for external Data. -> could add all balances, but i just want to bugfix instead, criteria fullfilled.
    }
}